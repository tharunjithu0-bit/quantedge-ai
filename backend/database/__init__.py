# The database package holds the database connection/session setup.
#
# db.py      - reusable SQLAlchemy engine + connection test helper
#
# Models (ORM classes) do NOT live here — they belong in models/.
# This package is only about the connection itself.
